/*
 * A collection of SVG files.
 *
 *
 * OMV Name: 17.0
 * OMV Time: 3-21-2022 16:34:19
 * OMV Title: Adobe InDesign 2022 (17.0) Object Model
 */
var SVGs = {


    /**
     * The number of objects in the collection.
     * @type {number}
     * @readonly
     */
    length: undefined,



    /**
     * Creates a new SVG.
     *
     * @param {Object} [withProperties] Initial values for properties of the new SVG (Optional)
     * @return SVG
     */
    add: function (withProperties) {
        
    },


    /**
     * Displays the number of elements in the SVG.
     *
     * @return number
     */
    count: function () {
        
    },


    /**
     * Returns the SVG with the specified index or name.
     *
     * @param {varies=any} [index] The index or name. Can accept: Long Integer or String.
     * @return SVG
     */
    item: function (index) {
        
    },


    /**
     * Returns the SVG with the specified name.
     *
     * @param {string} [name] The name.
     * @return SVG
     */
    itemByName: function (name) {
        
    },


    /**
     * Returns the SVG with the specified ID.
     *
     * @param {number} [id] The ID.
     * @return SVG
     */
    itemByID: function (id) {
        
    },


    /**
     * Returns the SVGs within the specified range.
     *
     * @param {varies=any} [from] The SVG, index, or name at the beginning of the range. Can accept: SVG, Long Integer or String.
     * @param {varies=any} [to] The SVG, index, or name at the end of the range. Can accept: SVG, Long Integer or String.
     * @return SVG
     */
    itemByRange: function (from, to) {
        
    },


    /**
     * Returns the first SVG in the collection.
     *
     * @return SVG
     */
    firstItem: function () {
        
    },


    /**
     * Returns the last SVG in the collection.
     *
     * @return SVG
     */
    lastItem: function () {
        
    },


    /**
     * Returns the middle SVG in the collection.
     *
     * @return SVG
     */
    middleItem: function () {
        
    },


    /**
     * Returns the SVG with the index previous to the specified index.
     *
     * @param {SVG} [obj] The index of the SVG that follows the desired SVG.
     * @return SVG
     */
    previousItem: function (obj) {
        
    },


    /**
     * Returns the SVG whose index follows the specified SVG in the collection.
     *
     * @param {SVG} [obj] The SVG whose index comes before the desired SVG.
     * @return SVG
     */
    nextItem: function (obj) {
        
    },


    /**
     * Returns any SVG in the collection.
     *
     * @return SVG
     */
    anyItem: function () {
        
    },


    /**
     * Returns every SVG in the collection.
     *
     * @return SVG
     */
    everyItem: function () {
        
    },


    /**
     * Generates a string which, if executed, will return the SVG.
     *
     * @return string
     */
    toSource: function () {
        
    },


    /**
     * Returns the SVG with the specified index.
     *
     * @param {number} [index] The index.
     * @return SVG
     */
    []: function (index) {
        
    },


};

